{
  "template_extensions": {
    "pytorch": {
      "commands": ["pip install torch torchvision torchaudio"]
    },
    "tensorflow": {
      "commands": ["pip install tensorflow", "pip install keras"]
    },
    "scikit-learn": {
      "commands": ["pip install scikit-learn", "pip install pandas numpy scipy"]
    }
  }
}
